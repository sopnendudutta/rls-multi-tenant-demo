# BRAHMO RLS Multi-Tenant Demo

This project demonstrates PostgreSQL Row-Level Security for multi-tenant healthcare knowledge isolation.

## Core Goal

The same SQL query:

```sql
SELECT * FROM knowledge_nodes;